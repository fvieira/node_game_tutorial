/**************************************************
** GAME PLAYER CLASS
**************************************************/
function Player(id, startX, startY) {
    this.id = id;
    this.x = startX;
    this.y = startY;
}

exports.Player = Player;
